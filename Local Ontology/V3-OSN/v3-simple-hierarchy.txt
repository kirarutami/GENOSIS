User (Class)
  ├── UserProfile (Sub-class)
  ├── PrivacySettings (Sub-class)
  │    └── StoryAndLiveSettings (Sub-class)
  └── SecuritySettings (Sub-class)
        └── LoginSecurity (Sub-sub-class)
            └── SessionManagement (Sub-sub-class)
        └── GeneralAccount (Sub-class)

ContentCreation (Class)
  ├── Posts (Sub-class)
  │    └── PhotoVideo (Sub-class)
  │    └── CheckIn (Sub-class)
  │    └── LiveVideo (Sub-class)
  │    └── LifeEvent (Sub-class)
  ├── Stories (Sub-class)
  │    └── StoryPhotoVideo (Sub-class)
  │    └── StoryHighlights (Sub-class)
  ├── Reels (Sub-class)
      └── ReelVideo (Sub-class)
      └── ReelMusic (Sub-class)

ContentInteraction (Class)
  ├── LikesReactions (Sub-class)
  │    └── Like (Sub-class)
  │    └── React (Sub-class)
  ├── Comments (Sub-class)
  │    └── TextComment (Sub-class)
  │    └── GIFComment (Sub-class)
  │    └── EmojiComment (Sub-class)

VideoContent (Class)
  ├── Videos (Sub-class)
  │    └── VideoNotifications (Sub-class)

ShortVideoContent (Class)
  ├── Reels (Sub-class)
      └── ReelSettings (Sub-class)

DirectMessages (Class)
  ├── TextMessage (Sub-class)
  ├── GroupChat (Sub-class)
  ├── MessageNotifications (Sub-class)
  ├── MessagePrivacy (Sub-class)
  ├── VideoCalls (Sub-class)
  └── VoiceCalls (Sub-class)

Marketplace (Class)
  ├── Listings (Sub-class)
  │    └── ListingDetails (Sub-class)
  ├── ListingsManagement (Sub-class)
  ├── MessageSeller (Sub-class)
  ├── TodaysPicks (Sub-class)
  └── SellerInformation (Sub-class)

Groups (Class)
  ├── GroupSettings (Sub-class)
  │    ├── PinnedGroups (Sub-class)
  │    └── GroupNotifications (Sub-class)
  ├── GroupRoles (Sub-class)
  ├── GroupFeatures (Sub-class)
  │    └── Events (Sub-class)
  │    └── Files (Sub-class)
  │    └── Photos (Sub-class)
  │    └── Polls (Sub-class)
  │    └── Announcements (Sub-class)
  ├── DiscoverGroups (Sub-class)
  │    └── GroupEvents (Sub-class)
  │    └── GroupInvites (Sub-class)
  ├── GroupInsights (Sub-class)
  │    └── MemberEngagement (Sub-class)
  │    └── GrowthMetrics (Sub-class)
  │    └── ContentPerformance (Sub-class)
  └── GroupModeration (Sub-class)
      ├── ModerationActions (Sub-class)
      └── Violations (Sub-class)

Pages (Class)
  ├── PagesManagement (Sub-class)
  │    └── PagesSettings (Sub-class)
  │    └── PagesFeatures (Sub-class)
  │    └── PagesInsights (Sub-class)
  │    └── PagesPromotions (Sub-class)
  ├── PagesActivities (Sub-class)
  ├── PagesInvites (Sub-class)
  └── PagesSearch (Sub-class)

HelpSupport (Class)

SettingsPrivacy (Class)
  ├── AccountSettings (Sub-class)
  │    ├── PrivacySettings (Sub-class)
  │    ├── SecuritySettings (Sub-class)
  │    ├── DataUsageSettings (Sub-class)
  │    ├── NotificationSettings (Sub-class)
  │    ├── AccountManagement (Sub-class)
  │    ├── AdPreferences (Sub-class)
  │    ├── LinkedAccounts (Sub-class)
  │    ├── PaymentSettings (Sub-class)
  │    ├── SecurityCheckup (Sub-class)
  │    └── DeviceManagement (Sub-class)

ProfessionalAccess (Class)

AlsoFromMeta (Class)
  ├── OtherApps (Sub-class)

ActivityLog (Class)

Logout (Class)
